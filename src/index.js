//import react
import React from 'react';
//import react dom
import ReactDOM from 'react-dom/client';
//import app compenent
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
